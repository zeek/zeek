before_install:
  - git submodule update --init --recursive
language: cpp
addons:
  apt:
    packages:
      - cmake
      - flex
      - bison
      - google-perftools
      - libpcap-dev
      - libssl-dev
      - python-dev
      - swig
      - zlib1g-dev
