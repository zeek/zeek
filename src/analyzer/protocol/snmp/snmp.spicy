# See the file "COPYING" in the main distribution directory for copyright.

module SNMP;

import ASN1;
import spicy;

type VersionTag = enum {
    SNMPV1_TAG = 0,
    SNMPV2_TAG = 1,
    SNMPV3_TAG = 3,
};

public type Message = unit {
    seqHeader: ASN1::ASN1Header;
};
