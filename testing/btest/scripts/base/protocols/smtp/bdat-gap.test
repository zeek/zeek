# @TEST-DOC: BDAT transaction with a gap
#
# @TEST-EXEC: zeek -b -r $TRACES/smtp/smtp-bdat-gap.pcap %INPUT >out
# @TEST-EXEC: zeek-cut -m uid id.orig_h id.orig_p id.resp_h id.resp_p service duration missed_bytes < conn.log > conn.log.cut
# @TEST-EXEC: zeek-cut -m uid fuid source depth mime_type filename sha1 missing_bytes < files.log > files.log.cut
# @TEST-EXEC: btest-diff out
# @TEST-EXEC: btest-diff conn.log.cut
# @TEST-EXEC: btest-diff smtp.log
# @TEST-EXEC: btest-diff files.log.cut
# @TEST-EXEC: btest-diff weird.log

@load base/protocols/conn
@load base/protocols/smtp
@load frameworks/files/hash-all-files

event smtp_request(c: connection, is_orig: bool, command: string, arg: string)
	{
	print c$uid, is_orig, command, arg;
	}
